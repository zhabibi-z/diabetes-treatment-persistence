# Development-only dependencies — not needed for the API or Streamlit deployment.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

# Jupyter notebooks (analysis, EDA, sensitivity)
jupyter==1.1.1
notebook==7.2.1
ipykernel==6.29.5
nbformat==5.10.4
nbconvert==7.17.1

# Linting and testing
ruff>=0.4.0
pytest>=8.0.0
pytest-cov>=5.0.0
